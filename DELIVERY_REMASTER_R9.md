# R9 · 游戏内战斗特效重制

完整项目源码，基于 GitHub R7 和已认可的 R8 本地交付。保持原版喷火、R8 三式剑技、规则和原画，重制弓箭、投枪、爪击、重击、元素/奥术/虚空法术、汲取及辅助效果。

## 直接运行
- `python3 build.py`：生成正常游戏单文件 `index.html` 和网络版 `dist/`。
- `python3 tools/vfx3/export_demo.py`：导出临时练习对局 `Card_Game_3D_VFX_Demo.html`。
- 打开演示，选择角色/法术，点击“实际出招”。上排通过真实规则执行；回放重建卡牌姿态与特效，但不重复扣血，不倒退游戏状态。
- “声音”由用户点击启用。原速回放有声音，慢放、暂停和拖轴会停止声音。
- “死亡”预设执行一次真实击杀，随后仅回看死亡余效。

## 架构与保留范围
`remaster-arts.js` / `remaster-cues.js` / `remaster-feedback.js` 是新增的特效几何、声音和姿态模块。沿用当前 WebGL Renderer；新旧后端按能力分流，保留原来的时序规划和失败回退。没有另做一款空场景游戏。

覆盖是按攻击家族与法术语义组织的：不同形态有不同运动、材质、接触和消退。不是为每张牌新建一套角色模型。抽牌、手牌、菜单及原 DOM 召唤/死亡流程没有推倒重写。

## 审阅资料
`output/remaster-r9/` 或独立交付的效果图、原速/半速 MP4 均来自程序运行。影片的音频由运行时相同 PCM 和事件时刻离线合成，不是现场扬声器录音。后半段仅为同画面局部裁切，没有概念图替换。

## 验证边界
浏览器采用 Xvfb/Chromium/ANGLE/Mesa 软件渲染、内存加载构建文件。手机横竖屏是窗口尺寸测试，不是实体手机。没有声称 Safari、真实 HTTP/file 来源存档、硬件帧率或完整端到端验收。帧采样视频的编码帧率不代表实时性能。原速正常游戏尺寸下的最终美术认可仍由实际体验决定。

本轮写入功能分支，不自动合并 main 或部署公开站点。远程最终提交见 PR，详细规格及测试见 `docs/design/FULL_VFX_REMASTER_R9.md`、`docs/qa/remaster-r9.json`。

## 本轮执行结果
- Node：495 项，487 通过、8 缺失源素材跳过、0 失败。
- 新特效：411 项定向 Chromium 检查通过；原标杆回归：102 项通过。
- 全部旧技能路由支持、新辅助语义、真实规则结果、目标归属、群体回放、终帧清理分别检查。数量不代表美术质量认证。

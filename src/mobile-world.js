/* Same approved source plates and palette as desktop, composed at native
 * mobile CSS coordinates by the shared cached renderer. */
const EmberMobileWorld={paint:(...args)=>AtelierWorld.paint(...args),get loading(){return AtelierWorld.loading;}};

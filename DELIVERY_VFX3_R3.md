# R3 · 焰流与重劈

## 版本和范围

以 `feat/3d-combat-vfx` 的 R2 提交 `7acce363395fdf22a1a0ac733139916aeb6630b1` 为基线。继续改进真实 card-game 的喷火和剑击，不另做游戏，不合并 main，不部署网站。

使用并核对了本会话交付的 `Ember_Steel_VFX_Source.zip`（《焰·弦·刃》），重点比较 `src/effects.js` 的出生时间驱动火焰与 `src/core.js` 的火焰着色器。R2 源码快照的 renderer/runtime Git blob SHA 与远端基线逐一匹配。

## 具体变化

### 喷火

原独立演示有较长的喷吐过程、单层橙金火焰材质、独立出生的火舌，以及完整三维角色和地面。游戏版 R2 不仅改变了时序，还把主体调暗后另叠亮芯，粒子到达末端后集中，形成亮带和亮团。本轮不是继续叠光圈：

- 恢复参考的橙金材质色值，取消重复白黄热芯薄片，增加有方向的多尺度噪声流动和内部纹理对比。
- 为每颗火舌保存出生时间、独立速度、生命周期、横向卷动和高度。接触处不用统一硬钳制，轴向运动连续减速，余焰向侧上方散开。
- 首次接触保持原判定时间，之后继续喷吐 680ms。停止发射不立即清空已经发出的粒子；整体在接触后 1450ms 以内清理。
- 降低火焰辉光、提高亮区提取阈值，避免多层叠加把纹理烧成纯黄色。每个材质实例携带自己的时钟，避免并发实例退场时剩余火焰纹理突然跳相位。
- 手机轻量画质删减部分粒子，但剩余粒子的轨迹、种子与原速时间不变。

本轮仍是原画龙卡的相对发射点，没有新增整条三维龙或独立地面灯光。它与参考使用相同类别的构建方法，但不是整场景像素一致，也不声称是物理流体或体积渲染。

### 空中重劈

- 利用现有攻击起手时间预启动，新增小幅回引、旋转和曲线下劈，不再主要依靠竖直平移。
- 刀剑使用固定尺寸的收尖网格，统一刚体变换；金属色、剑脊、护手和刃口保持自身轮廓。
- 剑尖按原 `contactAt` 接触目标脚下参考面。命中停留 45–70ms 后做小幅阻尼卸力，接触约 280ms 后再抬起撤出，不在命中帧跳回待机。
- 刃光采样同一把剑的历史位置，回引和下劈共用姿态函数。
- 裂缝从中心连续增长而不是整段突然出现；裂口、碎石、冲击光与尘土按不同时间收尾。

裂地是短暂覆盖层，不真实破坏棋盘，不把卡面拉伸撕裂。

### 接入

`src/effects.js` 让喷火/剑击实例在原 `beat.at` 创建，沿用原命中时间、力度、种子和单次规则结果。旧发射回调识别 `meshPrelude` 后只保留相应声音，不重复创建。闪电配方未变，无喷火同时播放时保持原有辉光参数。当前两种后端仍由原接入层受控路由。

## 构建与检查

```bash
python3 build.py
python3 tools/vfx3/export_demo.py
node --test tests/*.test.cjs
# Linux 图形检查（其他桌面系统直接使用图形会话）
xvfb-run -a python3 tools/vfx3/verify.py
xvfb-run -a python3 tools/vfx3/record_flow.py
```

`CHROMIUM_PATH` 可为录制工具指定本地浏览器。图形工具需要 Playwright 和 Chromium；普通构建仍只需要 Python 标准库，Node 测试使用内置模块。

- `index.html` / `dist/`：正常游戏，生产调试门禁未放开。
- `Card_Game_3D_VFX_Demo.html`：导出的临时练习对局，内存存储，上排调用真实攻击；时间轴仅回看三维特效，不重复扣血或重演 DOM 卡片动作。
- `tools/vfx3/record_flow.py`：执行真实规则动作后输出确定性特效回看帧，原速/半速；不录音。

## 测试与视觉检查

本轮新增 17 项 R3 测试（`tests/vfx3-flow.test.cjs`），覆盖独立火舌出生/消亡、首次接触、接触连续性、持续流覆盖、定种重放、时间压缩、几何稳定、剑尖固定、镜像姿态与预启动单实例。R2 测试中“剑在全部后段永远不转动”的断言改为“命中停留时稳定、随后在有界阻尼范围内卸力”；旧断言与这次用户要求的收势动作不兼容。

最终本地执行：214 项 Node 测试，206 通过、8 项因缺失源素材跳过、0 失败；35 项定向浏览器检查通过，包括三次真实攻击/施法的伤害 6/2/1、单实例路由、固定剑长、接触点、回拖不改变规则状态、清理、低画质、减少动态和手机横竖屏布局。完整记录在交付包 `output/vfx3-r3/`。GitHub CI 结果以 PR Checks 为准，不用本地记录代替远端运行。

已进行 R2 / R3、参考火焰及本轮多次实际浏览器截图走查。录像通过同一 runtime 按明确时刻求值生成；画面中使用了真实临时对局，但录像的时间轴只回看特效，血量不会再次变化。局部放大仅用于看清小尺寸技能，不是增加细节的超分辨率。录像没有音轨，正常游戏继续使用原音效设置。

## 边界

浏览器验证为 Xvfb、Chromium ANGLE/OpenGL、Mesa llvmpipe 软件渲染。基于运行环境限制在内存中加载源码，不是实际 HTTP 来源存档验证。没有运行全量 Playwright E2E，没有验收实体 Android/iPhone、Safari、硬件 GPU 帧率或多人并发对局压力。视频编码 FPS 不代表设备实时 FPS。跨 DOM 卡牌与三维覆盖层的空间遮挡仍有局限；不把自动测试通过当作所有美术效果均已达到商业品质。

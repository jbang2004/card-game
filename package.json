{
  "name": "emberfall-card-game",
  "version": "0.14.0",
  "private": true,
  "description": "Emberfall: single-player fantasy card battles, responsive UI and offline rules",
  "license": "MIT",
  "scripts": {
    "build": "python3 build.py",
    "test": "node --test tests/*.test.cjs",
    "test:e2e": "playwright test",
    "test:release": "npm run build && npm test && npm run test:art && npm run test:e2e",
    "serve": "python3 -m http.server 8000 --bind 127.0.0.1",
    "dev": "python3 build.py && python3 -m http.server 8000 --bind 127.0.0.1 --directory dist",
    "art:anime": "python3 tools/pack_card_assets.py",
    "art:atlas": "python3 tools/build_anime_assets.py",
    "art:world": "python3 tools/build_world_assets.py",
    "format": "prettier --write src/content src/rules src/presentation src/platform src/data.js src/engine.js src/ui.js",
    "test:art": "node --test tests/art/*.test.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "prettier": "^3.6.2"
  }
}

# R5 · 天剑贯星

基于已合并 R4 的 `9e5b062`。只重构剑击；已认可的原版火焰及其渲染材质保持逐字节不变。

## 查看
运行 `python3 build.py`、`python3 tools/vfx3/export_demo.py`。
`index.html` / `dist/` 为正式入口；`Card_Game_3D_VFX_Demo.html` 是内存存储的练习检查版。
点击“天剑·贯星”执行真实攻击。时间轴只回看三维特效，不重复伤害或重放所有 DOM 动作。

## 本轮效果
- 剑从屏幕上方之外、沿严格垂直方向高速贯入卡牌原画内。
- 没有回引、侧挥、命中前减速或插中后拔回。金属剑身、剑槽、护手、缠柄、宝石不拉伸。
- 单一接触点驱动短星芒、卡面暗裂缝、断边、分叉裂光与碎屑。
- 原攻击卡片不再同时冲进目标，避免表现成两次攻击。

## 实际执行
- Node 全套：264 项，256 通过，8 项因仓库没有美术源文件跳过，0 失败。
- 定向 Chromium 检查：47 项通过，含三种真实攻击/施法伤害 6/2/1、单个实例、竖直轨迹、尺寸恒定、精确接触、回放不改规则及三维层 PNG 一致性、清理、低画质、减少动态、手机横竖屏。
- 捕获七个关键帧、两张手机截图及108帧确定性实际渲染回放。24fps编码，前段原速，后段半速，无音轨。编码帧率不作为实时性能报告。
- `reference-flame.js`、`renderer.js` 的字节级保护测试通过；未修改规则、卡池或原画。

## 边界
实际运行于 Xvfb / Chromium / ANGLE / Mesa llvmpipe 软件 GPU。HTTP 导航被本次环境策略阻止，采用内存加载完整构建代码。
未执行全量 Playwright 端到端测试，未验收实体手机、Safari、文件来源存档或硬件 GPU 帧率。
裂纹与穿孔是短暂的覆盖层，非物理破坏。火焰质量未改动；不以功能测试代替用户的美术验收。

# 喷火与天降裂地重劈 · R2

本轮基于 `feat/3d-combat-vfx` 的 `54a5d5ffe60a395fd91c7f726ce386fb3dfeaf1f`，只修改喷火、剑劈及相应检查工具。闪电配方、伤害规则、卡牌原画、正式存档与主界面均保持原样。

## 实现

喷火取消连续锥体外壳，改用与独立火龙示例同类的出生时间驱动火焰薄片、噪声材质和深度排序。增加黄亮内焰与橙红外焰，第一次命中后持续发射约 480ms，再让已喷出的火自然消退。尾部最长 1100ms，不重复扣血。这里仍是从龙卡插画的相对发射点喷焰，没有把整条三维龙搬进战场，也不是流体模拟。

剑保持固定长度和比例，从目标上方略倾斜蓄势，以立方曲线加速下降。剑尖在既有命中时刻抵达目标脚下的参考平面，接触后固定，不弹回、不拉长。裂口从中心向两侧展开，包含暗缝、浅色断层、分叉裂光、碎石和尘土。地裂是短暂三维覆盖层，不是永久破坏棋盘或把卡面图片拆开；原来的攻击卡片运动没有改写。尾部最长 1000ms，不阻塞后续规则动作。

## 运行

```bash
python3 build.py
python3 tools/vfx3/export_demo.py
node --test tests/*.test.cjs
```

正常游戏是 `index.html` 或 `dist/`。检查版是 `Card_Game_3D_VFX_Demo.html`，上排按钮执行真实游戏攻击；时间轴仅回看三维特效，不能当作整段对局回放。检查版使用内存存储，不覆盖正式进度。

## 已执行验证

最终源码的完整 Node 单元测试：197 项，189 通过、8 项源素材缺失跳过、0 失败。新增 11 项覆盖下劈轨迹、刚性长度、落点、固定接触、裂纹展开与时间缩放等。

重新运行 `tools/vfx3/verify.py`：35 项定向 Chromium 检查通过。包括喷火/闪电/剑击真实规则动作各只生成一份三维实例，伤害分别为 6/2/1，回拖不改变对局状态，结束清理、低画质与减少动态，以及手机横竖屏布局。没有运行全量端到端套件。

浏览器使用 Xvfb + Chromium ANGLE/OpenGL 软件渲染。受环境导航限制，按现有检查器在内存加载完整构建源码，并仅在内存开放调试门禁。不构成实体手机、Safari、真实 HTTP 来源存档或硬件 GPU 性能验收。

## 演示复现

安装本地 Python Playwright 和可用 Chromium 后，在 Linux 显示环境执行：

```bash
xvfb-run -a python3 tools/vfx3/record_refinement.py
```

可通过 `CHROMIUM_PATH` 指定浏览器。输出在 `output/vfx3-r2/`：24fps 的 220 张帧，喷火与剑劈各有原速、半速的特效回看。导出视频还可将同一段作局部放大，不能把编码帧率当成实时性能。没有采集音轨；游戏音效仍沿用原开关与规则事件。

修改已准备提交到原功能分支与草稿 PR；不自动合并主分支，不发布网站。

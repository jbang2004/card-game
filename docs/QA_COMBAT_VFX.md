# 战斗视觉增强复验 · 2026-09-07

`npm run test:release`：构建成功，58 项 Node 测试通过，27 项 Playwright 测试通过（47.3 秒）。最后只格式化 CSS 并补充文档，再次构建成功，`git diff --check` 无错误。

新增三项浏览器测试覆盖桌面、390×844 触屏、传说牌真实出牌、减少动态设置中途切换、低画质粒子上限、取消和视口重排。既有回归覆盖 11 组桌面／触屏尺寸、满场与十张手牌、攻击／法术／AI、真实来源跨刷新 v1 存档、战役、收藏与触控旋转。

实测：演出尾段前后完整规则状态 JSON 相等；减少动态中途开启后随从只出现一次；自然播放结束及取消／重排后，活动动画、临时 DOM、粒子、待处理计时器均为 0，输入锁释放；低画质密集命中测试 Canvas 项数不超过 220。

人工检查截图（均由实际 Chromium 页面生成）：

- `artifacts/qa/legend-desktop.png`：金色传说铭牌、旋转法阵与落场反馈。
- `artifacts/qa/legend-touch.png`：触屏竖屏同一套效果，尺寸随视口缩小。
- `artifacts/qa/turn-desktop.png`：两排随从之间的紧凑旗帜。
- `artifacts/qa/turn-touch.png`：手机中央空隙的完整旗帜。
- `artifacts/qa/desktop.png` 和 `full-*.png`：常规与满场布局。

视觉策略：沿用现有胡桃木、古金酒馆与 56 张卡图；只增强事件反馈和状态轮廓。没有新增素材、依赖、Three.js 或规则执行路径。Canvas 活动效果预算为桌面 800、触屏 360、低画质 220；减少动态模式拒绝所有 Canvas 装饰效果，保留数字与静态状态提示。原 CSS 召唤动画和旧横幅的代码／样式已移除。

这些结果是系统 Chrome 的桌面和触屏模拟验证；未测试实体手机、Safari、长时间能耗或实际设备帧率。不将本次增强称为已达到《炉石传说》的整体制作规格。

# v0.11 架构升级验证

日期：2026-09-07。本记录替代当前开发入口中的历史源码哈希锁；旧版 QA 仍为历史证据。

## 已执行

- 构建：Python 标准库生成网页和离线单文件，无未解析模块占位符。
- Node：58 项通过，0 失败。包含 150 局随机完整对局、56 张卡迁移前固定局面对照、参数改动与文案/预览一致性、无效配置、因果块、只读快照、v1 存档、增益来源/期限与存储适配器。
- Playwright 全套：24 项通过，0 失败。覆盖实际出牌、攻击、AI、11 种满场尺寸、手机点牌确认和旋转、真实来源跨刷新存储、旧 v1 fixture 恢复、收藏/组牌、奖励推进、原画档案、生产只读接口及离线单文件。
- 最后复核修正了吸血武器攻击预览：英雄反伤后得到的治疗计入余血；增加独立单元测试。随后重新构建，并对实际战斗和生产接口运行针对性浏览器复验。
- `git diff --check` 无空白错误；Python 构建和资产脚本语法检查通过。
- 亲自查看桌面对战和手机收藏截图，保留原有场景、卡牌裁切与动态数值。其他尺寸也有自动截图和遮挡/边界断言。

## 对照证据

`tests/fixtures/card-behavior-v1.json` 从修改前引擎生成。相同初始状态下对照双方完整区域数据、RNG、实体编号、胜负及发现状态；新增 modifiers 元数据单独测试，不要求它存在于旧快照。

此对照不是全部卡牌组合的穷尽证明。AI 已改为理解效果参数的独立启发式策略，因此不要求 AI 与旧版做出完全相同的选择；AI 合法性、隐藏信息隔离和完整对局不变量仍持续测试。

`tests/fixtures/save-v1.json` 是旧版真实引擎序列化状态。Node 与浏览器都验证恢复，四个 localStorage 键不变。尚无实体手机/Safari/GPU 验收；当前只保留 Canvas，不再宣称有另一条 3D 路径。

## 清理效果

修改前使用 Git 输入版源码在独立临时目录重建，与最终构建比较：

| 产物 | 修改前 | 修改后 | 减少 |
|---|---:|---:|---:|
| 离线单文件 | 9,489,989 字节 | 5,891,066 字节 | 37.9% |
| 网页产物总量 | 7,278,925 字节 | 4,565,288 字节 | 37.3% |

网页产物从 129 个降至 100 个文件。56 张已确认卡图的文件哈希不变。删除旧渲染器、程序化卡图、转接层、未使用素材缓存和旧 Python 浏览器脚本；原始美术与历史记录保留但不加载。

运行产物报告：`artifacts/qa/architecture-v11.json`。完整浏览器报告：`artifacts/qa/full-results-v11.json`；最后针对性报告为 `artifacts/qa/results.json`。截图同目录。它们是可再生本地文件，不提交到源码。
